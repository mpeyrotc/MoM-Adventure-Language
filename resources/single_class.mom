specification Player {
    Int a();
};

class Character is_a Component of_type Player {
    field<Component> cc;

    Character() {
        parent();
    };

    Text name(Int a_number) {
        Real a_real = getWidth();
    };

    Int a() {
        Component c = new Component();
        Real r = c.getWidth();
        Real rr = cc.getHeight();
    };

    Nothing addCommonCard() {
    };
};


